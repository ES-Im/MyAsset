spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myasset_test
    username: test_root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jwt:
    secret: ${JWT_SECRET}


jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        use_sql_comments: true

